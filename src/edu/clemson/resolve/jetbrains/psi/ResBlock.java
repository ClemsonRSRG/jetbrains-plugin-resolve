package edu.clemson.resolve.jetbrains.psi;

/**
 * A general (dummy) interface for generic scopeblock in RESOLVE. Thus, this applies to the top level scope for an
 * instance of {@link ResModuleDecl} and also {@link ResOperationLikeNode}s.
 */
public interface ResBlock extends ResCompositeElement {
}
